<?php
// Import các tệp và cài đặt cần thiết ở đây

// Import UserController và UserModel
// require_once 'app/controllers/Auth.php';

// Tạo đối tượng UserController
// $auth = new Auth();

// Định nghĩa API routes
// $apiRoutes = array(
//     '/Project/Project_Pettu/auth/register' => 'Auth@register',
//     '/Project/Project_Pettu/auth/login' => 'Auth@login',
// );


// Xử lý các yêu cầu API
// foreach ($apiRoutes as $route => $controllerAction) {
//     $parts = explode('@', $controllerAction);
//     $controllerName = $parts[0];
//     $action = $parts[1];
    

//     if ($_SERVER['REQUEST_URI'] === $route && $_SERVER['REQUEST_METHOD'] === 'POST') {
//         // Gọi phương thức xử lý từ UserController
//         $auth->$action();
//         break;
//     }
// }

// Xử lý các API routes khác nếu cần
?>
